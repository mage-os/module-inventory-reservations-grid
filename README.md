# InventoryReservations Grid module

The `InventoryReservationsGrid` module provides a grid on admin panel for Inventory Reservations Module.


## How to install
`composer require mageos/module-inventory-reservations-grid`
